export const environment = {
  //if ng serve --env prod is executed, the api endpoint is set to the azure database.
  production: true,
  apiEndpoint: 'http://hotelexamproject.azurewebsites.net/api'
};
